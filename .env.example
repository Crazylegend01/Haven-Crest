# Required by server.js. Add the real value in Replit Secrets, not in source control.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional. Defaults to gemini-2.5-flash.
GEMINI_MODEL=gemini-2.5-flash

# Optional. Replit supplies PORT automatically.
PORT=3000